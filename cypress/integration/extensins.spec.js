/// <reference types="cypress" />

describe('', () => {
  it('', () => {
    cy.request({
      method: '',
      url: '',
      headers: { 'Accept-Language': 'en-us', },
    });
    cy.clickAlert()
  });
});
